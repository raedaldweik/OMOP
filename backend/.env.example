# Optional — without an API key, the agent runs in scenarios mode (canned demo answers).
# With a key, the LLM uses tool-use to drive every response.
ANTHROPIC_API_KEY=

# Optional model override (default: claude-sonnet-4-5-20250929)
# MODEL=claude-sonnet-4-5-20250929
